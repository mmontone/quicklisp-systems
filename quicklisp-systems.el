(defun quicklisp-apropos-systems (string)
  (interactive)
  )

(defun quicklisp-apropos-systems-names (string)
  (interactive)
  )

(defun quicklisp-load (system-name)
  (interactive))

(defalias 'quickload 'quicklisp-load)

(defun quicklisp-systems-download ()
  (interactive))

(defun quicklisp-systems ()
  (interactive)
  "Shows a buffer with all quicklisp systems"
  )

(provide 'quicklisp-systems)
